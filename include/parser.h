#ifndef PARSER_H
#define PARSER_H

#include "utility.h"
#include "vector.h"
#include "date.h"
#include "str.h"


/*
void date_strings(Date* date, vector* result);
void bool_strings(bool b, vector* result);
void trim(char* str, vector* result);
void erase_extra_spaces(char* str, vector* result);*/
size_t edit_distance(string first, string second);
//size_t get_document(string s);

#endif