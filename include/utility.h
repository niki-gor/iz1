#ifndef UTILITY_H
#define UTILITY_H

#include "types.h"
#include <string.h>
#include <stdlib.h>



void swap(void* a, void* b, size_t size);
size_t min(size_t a, size_t b);
size_t max(size_t a, size_t b);

#endif